diff --git a/higan/GNUmakefile b/higan/GNUmakefile
index 20effe8..c6c9603 100644
--- a/higan/GNUmakefile
+++ b/higan/GNUmakefile
@@ -16,7 +16,8 @@ ifeq ($(pgo),instrument)
   flags += -fprofile-generate
   link += -lgcov
 else ifeq ($(pgo),optimize)
-  flags += -fprofile-use
+  flags += -fprofile-use -fprofile-correction
+  link += -lgcov
 endif
 
 # platform
diff --git a/higan/target-loki/GNUmakefile b/higan/target-loki/GNUmakefile
index c21db45..9396dc5 100644
--- a/higan/target-loki/GNUmakefile
+++ b/higan/target-loki/GNUmakefile
@@ -1,6 +1,11 @@
 ifndef name
   name := loki
 endif
+
+ifndef DESTDIR
+  DESTDIR := /
+endif
+
 flags += -DDEBUGGER
 
 include sfc/GNUmakefile
@@ -59,18 +64,18 @@ install:
 ifeq ($(shell id -un),root)
 	$(error "make install should not be run as root")
 else ifneq ($(filter $(platform),linux bsd),)
-	mkdir -p $(prefix)/bin/
-	mkdir -p $(prefix)/share/icons/
-	mkdir -p $(prefix)/share/$(name)/
-	cp out/$(name) $(prefix)/bin/$(name)
-	cp data/higan.png $(prefix)/share/icons/$(name).png
-	cp -R profile/* $(prefix)/share/$(name)/
+	mkdir -p $(DESTDIR)$(prefix)/bin/
+	mkdir -p $(DESTDIR)$(prefix)/share/icons/
+	mkdir -p $(DESTDIR)$(prefix)/share/$(name)/
+	cp out/$(name) $(DESTDIR)$(prefix)/bin/$(name)
+	cp data/higan.png $(DESTDIR)$(prefix)/share/icons/$(name).png
+	cp -R profile/* $(DESTDIR)$(prefix)/share/$(name)/
 endif
 
 uninstall:
 ifeq ($(shell id -un),root)
 	$(error "make uninstall should not be run as root")
 else ifneq ($(filter $(platform),linux bsd),)
-	if [ -f $(prefix)/bin/$(name) ]; then rm $(prefix)/bin/$(name); fi
-	if [ -f $(prefix)/share/icons/$(name).png ]; then rm $(prefix)/share/icons/$(name).png; fi
+	if [ -f $(DESTDIR)$(prefix)/bin/$(name) ]; then rm $(DESTDIR)$(prefix)/bin/$(name); fi
+	if [ -f $(DESTDIR)$(prefix)/share/icons/$(name).png ]; then rm $(DESTDIR)$(prefix)/share/icons/$(name).png; fi
 endif
diff --git a/higan/target-star-rod/GNUmakefile b/higan/target-star-rod/GNUmakefile
index a8e5055..6935618 100644
--- a/higan/target-star-rod/GNUmakefile
+++ b/higan/target-star-rod/GNUmakefile
@@ -2,6 +2,10 @@ ifndef name
   name := star-rod
 endif
 
+ifndef DESTDIR
+  DESTDIR := /
+endif
+
 flags += -DDEBUGGER
 
 ifndef profile
@@ -92,13 +96,13 @@ else ifeq ($(platform),macosx)
 	cp -R out/$(name).app /Applications/$(name).app
 	cp -R systems/* ~/Library/Application\ Support/$(name)/
 else ifneq ($(filter $(platform),linux bsd),)
-	mkdir -p $(prefix)/bin/
-	mkdir -p $(prefix)/share/icons/
-	mkdir -p $(prefix)/share/$(name)/
-	cp out/$(name) $(prefix)/bin/$(name)
-	cp data/$(name).desktop $(prefix)/share/applications/$(name).desktop
-	cp data/$(name).png $(prefix)/share/icons/$(name).png
-	cp -R systems/* $(prefix)/share/$(name)/
+	mkdir -p $(DESTDIR)$(prefix)/bin/
+	mkdir -p $(DESTDIR)$(prefix)/share/icons/
+	mkdir -p $(DESTDIR)$(prefix)/share/$(name)/
+	cp out/$(name) $(DESTDIR)$(prefix)/bin/$(name)
+	cp data/$(name).desktop $(DESTDIR)$(prefix)/share/applications/$(name).desktop
+	cp data/$(name).png $(DESTDIR)$(prefix)/share/icons/$(name).png
+	cp -R systems/* $(DESTDIR)$(prefix)/share/$(name)/
 endif
 
 uninstall:
@@ -108,7 +112,7 @@ else ifeq ($(platform),windows)
 else ifeq ($(platform),macosx)
 	if [ -d /Applications/$(name).app ]; then rm -r /Applications/$(name).app; fi
 else ifneq ($(filter $(platform),linux bsd),)
-	if [ -f $(prefix)/bin/$(name) ]; then rm $(prefix)/bin/$(name); fi
-	if [ -f $(prefix)/share/applications/$(name).desktop ]; then rm $(prefix)/share/applications/$(name).desktop; fi
-	if [ -f $(prefix)/share/icons/$(name).png ]; then rm $(prefix)/share/icons/$(name).png; fi
+	if [ -f $(DESTDIR)$(prefix)/bin/$(name) ]; then rm $(DESTDIR)$(prefix)/bin/$(name); fi
+	if [ -f $(DESTDIR)$(prefix)/share/applications/$(name).desktop ]; then rm $(DESTDIR)$(prefix)/share/applications/$(name).desktop; fi
+	if [ -f $(DESTDIR)$(prefix)/share/icons/$(name).png ]; then rm $(DESTDIR)$(prefix)/share/icons/$(name).png; fi
 endif
diff --git a/higan/target-tomoko/GNUmakefile b/higan/target-tomoko/GNUmakefile
index b5d33c5..578e9a9 100644
--- a/higan/target-tomoko/GNUmakefile
+++ b/higan/target-tomoko/GNUmakefile
@@ -1,6 +1,11 @@
 ifndef name
   name := higan
 endif
+
+ifndef DESTDIR
+  DESTDIR := /
+endif
+
 flags += -DSFC_SUPERGAMEBOY
 
 include a2600/GNUmakefile
@@ -97,14 +102,14 @@ else ifeq ($(platform),macosx)
 	cp data/cheats.bml ~/Library/Application\ Support/$(name)/
 	cp -R systems/* ~/Library/Application\ Support/$(name)/
 else ifneq ($(filter $(platform),linux bsd),)
-	mkdir -p $(prefix)/bin/
-	mkdir -p $(prefix)/share/icons/
-	mkdir -p $(prefix)/share/$(name)/
-	cp out/$(name) $(prefix)/bin/$(name)
-	cp data/$(name).desktop $(prefix)/share/applications/$(name).desktop
-	cp data/$(name).png $(prefix)/share/icons/$(name).png
-	cp data/cheats.bml $(prefix)/share/$(name)/cheats.bml
-	cp -R systems/* $(prefix)/share/$(name)/
+	mkdir -p $(DESTDIR)$(prefix)/bin/
+	mkdir -p $(DESTDIR)$(prefix)/share/icons/
+	mkdir -p $(DESTDIR)$(prefix)/share/$(name)/
+	cp out/$(name) $(DESTDIR)$(prefix)/bin/$(name)
+	cp data/$(name).desktop $(DESTDIR)$(prefix)/share/applications/$(name).desktop
+	cp data/$(name).png $(DESTDIR)$(prefix)/share/icons/$(name).png
+	cp data/cheats.bml $(DESTDIR)$(prefix)/share/$(name)/cheats.bml
+	cp -R systems/* $(DESTDIR)$(prefix)/share/$(name)/
 endif
 
 uninstall:
@@ -114,7 +119,7 @@ else ifeq ($(platform),windows)
 else ifeq ($(platform),macosx)
 	if [ -d /Applications/$(name).app ]; then rm -r /Applications/$(name).app; fi
 else ifneq ($(filter $(platform),linux bsd),)
-	if [ -f $(prefix)/bin/$(name) ]; then rm $(prefix)/bin/$(name); fi
-	if [ -f $(prefix)/share/applications/$(name).desktop ]; then rm $(prefix)/share/applications/$(name).desktop; fi
-	if [ -f $(prefix)/share/icons/$(name).png ]; then rm $(prefix)/share/icons/$(name).png; fi
+	if [ -f $(DESTDIR)$(prefix)/bin/$(name) ]; then rm $(DESTDIR)$(prefix)/bin/$(name); fi
+	if [ -f $(DESTDIR)$(prefix)/share/applications/$(name).desktop ]; then rm $(DESTDIR)$(prefix)/share/applications/$(name).desktop; fi
+	if [ -f $(DESTDIR)$(prefix)/share/icons/$(name).png ]; then rm $(DESTDIR)$(prefix)/share/icons/$(name).png; fi
 endif
diff --git a/icarus/GNUmakefile b/icarus/GNUmakefile
index fc201db..98c9fdd 100644
--- a/icarus/GNUmakefile
+++ b/icarus/GNUmakefile
@@ -1,7 +1,14 @@
 include ../nall/GNUmakefile
 include ../hiro/GNUmakefile
 
-name := icarus
+ifndef name
+  name := icarus
+endif
+
+ifndef DESTDIR
+  DESTDIR := /
+endif
+
 flags += -I.. -O3
 link +=
 
@@ -42,14 +49,14 @@ install:
 ifeq ($(platform),macosx)
 	cp -R out/$(name).app /Applications/$(name).app
 else ifneq ($(filter $(platform),linux bsd),)
-	mkdir -p $(prefix)/share/$(name)/Database/
-	if [ -f out/$(name) ]; then cp out/$(name) $(prefix)/bin/$(name); fi
-	cp -R Database/ $(prefix)/share/$(name)/Database/
+	mkdir -p $(DESTDIR)$(prefix)/share/$(name)/Database/
+	if [ -f out/$(name) ]; then cp out/$(name) $(DESTDIR)$(prefix)/bin/$(name); fi
+	cp -R Database/ $(DESTDIR)$(prefix)/share/$(name)/Database/
 endif
 
 uninstall:
 ifeq ($(platform),macosx)
 	if [ -d /Applications/$(name).app ]; then rm -r /Applications/$(name).app; fi
 else ifneq ($(filter $(platform),linux bsd),)
-	if [ -f $(prefix)/bin/$(name) ]; then rm $(prefix)/bin/$(name); fi
+	if [ -f $(DESTDIR)$(prefix)/bin/$(name) ]; then rm $(DESTDIR)$(prefix)/bin/$(name); fi
 endif
diff --git a/nall/GNUmakefile b/nall/GNUmakefile
index 59f07f6..94e1519 100644
--- a/nall/GNUmakefile
+++ b/nall/GNUmakefile
@@ -51,9 +51,9 @@ ifeq ($(compiler),)
   else ifeq ($(platform),macosx)
     compiler := clang++
   else ifeq ($(platform),linux)
-    compiler := g++-4.9
+    compiler := g++
   else ifeq ($(platform),bsd)
-    compiler := g++49
+    compiler := g++
   else
     compiler := g++
   endif
@@ -99,7 +99,9 @@ ifeq ($(threaded),true)
 endif
 
 # paths
-prefix := $(HOME)/.local
+ifndef prefix
+  prefix := $(HOME)/.local
+endif
 
 # function rwildcard(directory, pattern)
 rwildcard = \
diff --git a/shaders/GNUmakefile b/shaders/GNUmakefile
index 80d315c..6d5194a 100644
--- a/shaders/GNUmakefile
+++ b/shaders/GNUmakefile
@@ -1,7 +1,15 @@
+ifndef name
+  name := higan
+endif
+
+ifndef DESTDIR
+  DESTDIR := /
+endif
+
 install:
-	if [ -d /usr/share/higan/Video\ Shaders ]; then sudo rm -r /usr/share/higan/Video\ Shaders; fi
-	sudo mkdir -p /usr/share/higan/Video\ Shaders
-	sudo cp -r *.shader /usr/share/higan/Video\ Shaders
-	sudo mkdir -p /usr/share/higan/Video\ Shaders/Display\ Emulation
-	sudo cp -r /Display\ Emulation/*.shader /usr/share/higan/Video\ Shaders/Display\ Emulation
-	sudo chmod -R 777 /usr/share/higan/Video\ Shaders
+	if [ -d $(DESTDIR)usr/share/$(name)/Video\ Shaders ]; then rm -r $(DESTDIR)usr/share/$(name)/Video\ Shaders; fi
+	mkdir -p $(DESTDIR)usr/share/$(name)/Video\ Shaders
+	cp -r *.shader $(DESTDIR)usr/share/$(name)/Video\ Shaders
+	mkdir -p $(DESTDIR)/usr/share/$(name)/Video\ Shaders/Display\ Emulation
+	cp -r /Display\ Emulation/*.shader $(DESTDIR)/usr/share/$(name)/Video\ Shaders/Display\ Emulation
+	chmod -R 777 $(DESTDIR)usr/share/$(name)/Video\ Shaders
