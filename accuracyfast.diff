diff --git a/higan/Makefile b/higan/Makefile
index 65ad25d..08df7eb 100755
--- a/higan/Makefile
+++ b/higan/Makefile
@@ -6,7 +6,7 @@ gb  := gb
 gba := gba
 # nds := nds
 
-profile := accuracy
+profile := accuracyfast
 target  := ethos
 
 # options += debugger
diff --git a/higan/sfc/Makefile b/higan/sfc/Makefile
index 806cf6d..f83bfc8 100755
--- a/higan/sfc/Makefile
+++ b/higan/sfc/Makefile
@@ -13,6 +13,12 @@ ifeq ($(profile),accuracy)
   flags += -DPROFILE_ACCURACY
   sfccpu := $(sfc)/cpu
   sfcsmp := $(sfc)/smp
   sfcdsp := $(sfc)/dsp
   sfcppu := $(sfc)/ppu
+else ifeq ($(profile),accuracyfast)
+  flags += -DPROFILE_ACCURACYFAST
+  sfccpu := $(sfc)/cpu
+  sfcsmp := $(sfc)/smp
+  sfcdsp := $(sfc)/alt/dsp
+  sfcppu := $(sfc)/ppu
 else ifeq ($(profile),balanced)
   flags += -DPROFILE_BALANCED
   sfccpu := $(sfc)/cpu
diff --git a/higan/sfc/profile-accuracyfast.hpp b/higan/sfc/profile-accuracyfast.hpp
index e69de29..dd0319c 100644
--- a/higan/sfc/profile-accuracyfast.hpp
+++ b/higan/sfc/profile-accuracyfast.hpp
@@ -0,0 +1,12 @@
+namespace Info {
+  static const char Profile[] = "Accuracyfast";
+}
+
+#if defined(DEBUGGER)
+  #error "bsnes: debugger not supported with accuracyfast profile."
+#endif
+
+#include <sfc/cpu/cpu.hpp>
+#include <sfc/smp/smp.hpp>
+#include <sfc/alt/dsp/dsp.hpp>
+#include <sfc/ppu/ppu.hpp>
diff --git a/higan/sfc/sfc.hpp b/higan/sfc/sfc.hpp
index b7167f1..2d62f4f 100755
--- a/higan/sfc/sfc.hpp
+++ b/higan/sfc/sfc.hpp
@@ -57,6 +57,8 @@ namespace SuperFamicom {
 
   #if defined(PROFILE_ACCURACY)
   #include "profile-accuracy.hpp"
+  #elif defined(PROFILE_ACCURACYFAST)
+  #include "profile-accuracyfast.hpp"
   #elif defined(PROFILE_BALANCED)
   #include "profile-balanced.hpp"
   #elif defined(PROFILE_PERFORMANCE)
